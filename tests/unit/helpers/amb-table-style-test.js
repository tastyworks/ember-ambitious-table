import { ambTableStyle } from 'dummy/helpers/amb-table-style';
import { module, test } from 'qunit';

module('Unit | Helper | amb table style');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = ambTableStyle([42]);
  assert.ok(result);
});
